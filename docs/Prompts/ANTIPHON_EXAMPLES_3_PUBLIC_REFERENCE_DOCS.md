> Generated by Wolfgang — Antiphon's NuGet package README currently links five doc pages into the *private* `Elindryn/Antiphon` repository, which 404s/access-denies for every real customer, and which (worse) sits in the same repo as every internal implementation prompt and AI completion report. This phase authors genuinely public-safe replacements here, in the already-public `antiphon-examples`. A follow-up Antiphon-side phase (`ANT-SDK-11`) will repoint the package README at these once merged.
> Do not edit this header.

# EX-3 — Public Reference Documentation

**Project:** antiphon-examples
**Phase:** EX-3
**Date:** 2026-09-17

---

## How to Execute This Prompt

Open a **new Claude Code session launched from the antiphon-examples repository root** (`origin` = the private `Elindryn/Antiphon.Example`, the actual dispatch target — do not touch the `public` remote or its sync process, that stays a separate manual step after merge).

Do not run this prompt from within a Wolfgang Claude session.

---

## Goal

Five new, genuinely public-safe documents exist in this repo — `How It Works`, `How It Was Tested`, `How To Run Examples`, `Licensing`, `Support Terms` — written for an external developer evaluating the Antiphon SDK, grounded only in what's true and verifiable from this repo's own content and public information. This repo's `README.md` links to all five prominently.

---

## Project Context

This repo (`antiphon-examples`) hosts three real, runnable sample console projects demonstrating the Antiphon e-invoicing SDK (`README.md`, `PACKAGES.md` already exist and are accurate as of `EX-2`, which swapped the samples onto the real published `nuget.org` `1.0.0` packages).

Antiphon's own private repository (`Elindryn/Antiphon`, which this session does **not** have access to and must not assume access to) currently has a `Directory.Build.targets` template that generates every NuGet package's README with links to five docs inside that same private repo: `Antiphon-Getting-Started.md`, `Antiphon-Rule-Reference.md`, `Antiphon-Versioning-Policy.md`, `Antiphon-Licensing.md`, `Antiphon-Support-Terms.md`. Every one of those links 404s or access-denies for a real customer, since the repo is private. One of them (`Antiphon-Versioning-Policy.md`) was found to contain internal phase codenames (`ANT-STD-3`, etc.) baked directly into its prose — evidence those docs were written for internal engineering use, not external readers, and should not simply be copied.

This phase writes fresh, customer-facing replacements here instead — not copies of the private docs. A subsequent, separate Antiphon-side phase will repoint the package README's links at whatever this phase produces, once merged.

Relevant files already in this repo:
- `README.md` — the existing getting-started flow, release-posture note, and links
- `PACKAGES.md` — the existing package-architecture map
- `docs/Results/EX-1-Console-Samples.md` — contains the real, already-verified veraPDF PDF/A-1b (129/129) and PDF/A-3b (146/146) conformance numbers for this repo's own `sample-input.pdf`/`facturx-output.pdf` — a legitimate, traceable source for "How It Was Tested" claims
- `docs/Results/EX-2-Real-Nuget-Swap.md` — confirms the packages are live on `nuget.org` at `1.0.0` under evaluation mode

---

## Important Instructions

Do **not**:

- Assume or fabricate access to the private `Elindryn/Antiphon` repository's internal docs. Ground every claim in this repo's own existing content (samples, README, PACKAGES.md, docs/Results/) or genuinely public information (the nuget.org package listings, the public validator site if referenced elsewhere in this repo).
- Mention Wolfgang, Claude Code, AI-assisted development, dispatch, Codex, or any internal phase codename (`ANT-*`, `SDK-*`, `EX-*`) anywhere in the five new documents. They must read as if written by a human technical writer for an external audience.
- Describe the full commercial/paid-tier EULA in detail — it is not reviewed by counsel yet and is not the public story. State plainly that evaluation mode is what's live today; commercial licensing is available on inquiry.
- Invent a contact method, support channel, or claim not traceable to this repo's own existing content. If something needed for a doc genuinely isn't available, say so in the completion report rather than inventing it.
- Link from any of the five new documents into `docs/Prompts/` or `docs/Results/` (this repo's own, or any other repo's).
- Change `PackageReference` versions or IDs in any of the three sample `.csproj` files — that is a separate, subsequent phase's job.
- Modify sibling repositories or Wolfgang.

### Git and working-tree discipline (standing rule — do not deviate)

This session may be running inside an **auto-created Git worktree** on a `worktree-*` branch. That is expected and correct — work with the files exactly as they are checked out.

- **Leave every change uncommitted in the working tree.** Do not `git add`/`git commit`, do not `git push`, do not create branches, do not `gh pr create` / `gh pr merge` / `gh pr ready`.
- **Do not move `HEAD` or rewrite history.** No `git reset`, `git checkout <ref>` / `git switch`, `git rebase`, `git merge`, `git pull`, `git fetch` followed by a reset, `git cherry-pick`, `git revert`, `git stash`, `git branch -D/-f/-m`, `git update-ref`, `git gc`, `git reflog expire`. `git status`, `git diff`, `git log`, `git show`, and a plain `git branch` listing are fine.
- **If the checked-out base looks wrong** — STOP and say so in the completion report. Do not try to correct it with git.
- A `PreToolUse` hook denies the git commands above and fails closed. Do not attempt to work around, disable, or overwrite it.

I will handle all source control myself or via the dispatcher.

---

## Read Before Changing

- `README.md`
- `PACKAGES.md`
- `docs/Results/EX-1-Console-Samples.md`
- `docs/Results/EX-2-Real-Nuget-Swap.md`
- The three sample projects' own source and READMEs, if any exist beyond the root `README.md`

---

## Scope

### 1. `docs/Antiphon-How-It-Works.md`

An architecture/capability overview: what Antiphon validates and generates (EN16931 core, Peppol BIS UBL/CII, XRechnung UBL/CII, CEN UBL/CII, Factur-X/PDF generation — confirm this list against what `PACKAGES.md` actually documents rather than assuming it's exactly this list), and how the packages are organized at a high level (point to `PACKAGES.md` for the detailed map rather than duplicating it). Written for a developer deciding whether to evaluate the SDK.

### 2. `docs/Antiphon-How-It-Was-Tested.md`

A conformance/test-coverage summary in customer-facing terms, grounded in this repo's own verified results — e.g. the real veraPDF PDF/A-1b and PDF/A-3b numbers from `docs/Results/EX-1-Console-Samples.md`, and a plain statement that the SDK validates against official standards-body example corpora (EN16931, Peppol, XRechnung) without naming internal phase work. If a specific claim can't be traced to this repo's own content or genuinely public information, don't make it.

### 3. `docs/Antiphon-How-To-Run-Examples.md`

Extract and clean up this repo's own existing "Getting started"/"Running the samples" flow from `README.md` into its own standalone document, so the package README can link directly to it. `README.md` should keep a short pointer to this new doc rather than duplicating the full instructions in both places (your call on exactly how much stays inline vs. moves — avoid leaving the same instructions maintained in two places).

### 4. `docs/Antiphon-Licensing.md`

A customer-facing summary of evaluation-mode licensing: no license key required, full function, permanently, notice-only — grounded in this repo's own README's existing "Release posture" section. State plainly that evaluation mode is what's live today; commercial/paid licensing is available on inquiry via whatever contact method already exists in this repo's own content (e.g. a website URL already referenced in `README.md`) — do not invent one if none exists, say so in the completion report instead.

### 5. `docs/Antiphon-Support-Terms.md`

A short, honest statement of support posture matching evaluation-mode reality — no SLA implied. Grounded only in what's already true/public.

### 6. Update `README.md`

Add prominent links to all five new documents, alongside the existing `PACKAGES.md` reference.

---

## Files Allowed to Change

- `docs/Antiphon-How-It-Works.md` (new)
- `docs/Antiphon-How-It-Was-Tested.md` (new)
- `docs/Antiphon-How-To-Run-Examples.md` (new)
- `docs/Antiphon-Licensing.md` (new)
- `docs/Antiphon-Support-Terms.md` (new)
- `README.md`

This is the permitted change set. If you discover a necessary change outside this list, stop and report it rather than expanding scope silently.

---

## Files and Folders Out of Scope

- Do not modify `PACKAGES.md`.
- Do not modify any sample's `.csproj` or `.cs` source files.
- Do not modify `docs/Results/` or `docs/Prompts/` (any file).
- Do not modify `NuGet.config` or `LICENSE`.
- Do not modify the `public` remote, `public-export` branch, or any dual-remote sync tooling.
- Do not modify sibling repositories (including `../Antiphon`) or Wolfgang.
- Do not commit or push.

---

## Verification Steps

This phase is documentation-only — no build/test verification applies. Instead:

1. Read all five new documents in full after writing them and confirm none contains: an internal phase codename (grep for `ANT-`, `SDK-`, `EX-`, `Wolfgang`, `Claude`, `Codex`, `dispatch`), a link into `docs/Prompts/` or `docs/Results/`, or an invented claim not traceable to this repo's own content.
2. Confirm `README.md`'s new links resolve to the correct relative paths.

Report the exact grep commands run and their (empty, confirming clean) output in the completion report.

---

## Post-Completion Review (bounded — one round)

After verification passes, run **one** Codex review of the full diff for this phase. Then apply exactly **one** fix pass under these rules, and stop:

1. **Fix only findings with a concrete failure scenario** — here, that means: an internal-process leak, a fabricated claim, or a broken link. Style/tone preferences are recorded, never acted on in this pass.
2. **One fix pass, no re-review loop.**
3. **Fixes stay inside the "Files Allowed to Change" list.**
4. If Codex is unavailable, say so in the completion report and skip this section.

---

## Documentation Updates

- [ ] `README.md` is itself the primary documentation target of this phase (see Scope §6).
- [ ] If no other documentation change was needed, state that explicitly in the completion report.

---

## Result File

Save a copy of your completion report to:

```
docs/Results/EX-3-Public-Reference-Docs.md
```

---

## Completion Report

When finished, report:

1. Summary — what changed and why.
2. Safety checks or pre-flight inspection results (if applicable).
3. Files changed — added, updated, or deleted.
4. Files not changed — list any from the "Files Allowed to Change" set that turned out not to need changes, and why.
5. Documentation changes — what was reviewed and what was updated.
6. Behavior and contract impact — confirm none (documentation-only phase).
7. Database impact — not applicable.
8. Verification results — the grep commands run per the Verification Steps and their output.
9. Review outcome — findings fixed, findings recorded-but-not-fixed, or "review skipped: [reason]".
10. Out-of-scope items discovered — flag but do not implement. Specifically: was any claim in the "Scope" section above impossible to ground in real, traceable information? Name exactly what and why.
11. Suggested follow-up tasks — name them; Wolfgang will track them.

---

## After This Prompt Executes

Bring the completion report back to Wolfgang. Wolfgang will update:

- `projects/antiphon.md` — project card
- `ecosystem/ACTIVE_PRIORITIES.md`
- `prompts/PROMPT_LOG.md`
- `NOW.md`
- Generate the follow-up Antiphon-side phase (`ANT-SDK-11`) that repoints the package README's doc links at these five new documents and performs the `Antiphon.Invoicing.*` package-ID rename
