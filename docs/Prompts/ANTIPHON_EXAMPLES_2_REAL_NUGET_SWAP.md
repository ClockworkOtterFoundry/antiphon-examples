> Generated by Wolfgang — Antiphon v1.0.0 published to nuget.org 2026-09-16 (all 19 packages, org `clockworkotterfoundryllc`, tagged on `../Antiphon` `main` at `51667c0`). This is `EX-1`'s own already-planned follow-up (fu-3a) — swap the local package feed for the real thing now that it exists.
> Do not edit this header.

# EX-2 — Swap Local NuGet Feed For Real Published Packages

**Project:** antiphon-examples
**Phase:** EX-2
**Date:** 2026-09-16

---

## How to Execute This Prompt

Open a **new Claude Code session launched from the antiphon-examples repository root** (this repo's `origin` remote — the private `Elindryn/Antiphon.Example` — is what you'll be working in; do not touch the `public` remote or its sync process, that is a separate manual step handled after merge).

Do not run this prompt from within a Wolfgang Claude session, even if that session has file access to this repository.

---

## Goal

All three sample projects (`01-generate-xrechnung`, `02-validate-peppol`, `03-facturx-pdf`) build and run using only `dotnet restore`/`dotnet run` against the real `nuget.org` feed at `Antiphon.*` version `1.0.0` — no local feed, no sibling `../Antiphon` checkout required. `NuGet.config`, `README.md`, and `PACKAGES.md` all reflect this as the actual, current state, not a future plan.

---

## Project Context

This is a small, public-facing (via a separate `public` remote) sample-code repository demonstrating Antiphon SDK usage. It was scaffolded in `EX-1` (`docs/Results/EX-1-Console-Samples.md`) against a local, gitignored `local-feed/` package source because Antiphon wasn't published anywhere yet at the time. That blocker is now closed: Antiphon `v1.0.0` was pushed to `nuget.org` for real on 2026-09-16, under NuGet.org organization `clockworkotterfoundryllc`. All 19 package IDs (`Antiphon`, `Antiphon.Generation.Ubl`, `Antiphon.Generation.Cii`, `Antiphon.Validation.Pipeline`, `Antiphon.Rules.Cen.Ubl`, `Antiphon.Rules.Peppol.Ubl`, `Antiphon.Xsd.Ubl`, `Antiphon.Pdf`, and others) are live at version `1.0.0`.

Relevant files:
- `README.md` — has a full "Local-feed setup" section and a "Release posture" section, both written when nuget.org publish was still pending
- `PACKAGES.md` — has a "Not on nuget.org yet" section
- `NuGet.config` — has a `local-feed` package source alongside `nuget.org`
- `docs/Results/EX-1-Console-Samples.md` — historical record of the original scaffold; **do not edit**, it correctly describes what was true when it was written

---

## Important Instructions

Do **not**:

- Edit `docs/Results/EX-1-Console-Samples.md` or any other existing file under `docs/Results/` — that is historical record, not to be updated to reflect current state.
- Change any sample's `.cs` source files — the public API surface at `1.0.0` is unchanged from what these samples were written against; this is a metadata/config swap only. If you discover the API genuinely changed and a sample no longer compiles for a reason other than a version-string bump, STOP and report it rather than patching around it.
- Touch the `public` git remote, the `public-export` branch, or anything related to syncing this repo's private/public split — that is a separate manual step Wolfgang/the user handles after this phase merges.
- Modify Wolfgang.

I will handle deployment/publishing of this repo's own `public` mirror manually.

### Git and working-tree discipline (standing rule — do not deviate)

This session may be running inside an **auto-created Git worktree** on a `worktree-*` branch. That is expected and correct — work with the files exactly as they are checked out.

- **Leave every change uncommitted in the working tree.** Do not `git add`/`git commit`, do not `git push`, do not create branches, do not `gh pr create` / `gh pr merge` / `gh pr ready`. Committing and PR creation are the dispatcher's job or a human's — never this session's.
- **Do not move `HEAD` or rewrite history.** No `git reset`, `git checkout <ref>` / `git switch`, `git rebase`, `git merge`, `git pull`, `git fetch` followed by a reset, `git cherry-pick`, `git revert`, `git stash`, `git branch -D/-f/-m`, `git update-ref`, `git gc`, `git reflog expire`. `git status`, `git diff`, `git log`, `git show`, and a plain `git branch` listing are fine.
- **If the checked-out base looks wrong** — stale, missing a commit or file the phase depends on, on an unexpected branch — **STOP and say so in the completion report** (and block for input if running unattended). Do **not** try to correct it with git.
- A `PreToolUse` hook denies the git commands above and fails closed. Do not attempt to work around, disable, or overwrite it.

I will handle all source control (branching, committing, PRs, merges) myself or via the dispatcher.

---

## Read Before Changing

Inspect these before making any changes:

- `NuGet.config`
- `01-generate-xrechnung/01-generate-xrechnung.csproj`
- `02-validate-peppol/02-validate-peppol.csproj`
- `03-facturx-pdf/03-facturx-pdf.csproj`
- `README.md` (full "Local-feed setup" and "Release posture" sections)
- `PACKAGES.md` (the "Not on nuget.org yet" section and anywhere else it references package availability)

---

## Scope

### 1. Remove the local feed

In `NuGet.config`, remove the `antiphon-local` package source entry and its explanatory `<!-- -->` comment. Leave `nuget.org` as the only source. If a `local-feed/` directory exists in the working tree (it's gitignored, so it may or may not be present locally), delete it — it should play no role in verification.

### 2. Pin real published versions

In each of the three `.csproj` files, change every `<PackageReference Include="Antiphon...">`'s `Version="0.9.0"` to `Version="1.0.0"` — the actual version published to nuget.org. Do not change anything else in these files (target framework, other package references, etc.).

### 3. Update README.md

- Remove the "Local-feed setup" section's instructions for building `../Antiphon` locally and copying `.nupkg`/`.snupkg` files into a `local-feed/` folder. Replace it with the actual simple path: clone this repo, `dotnet restore`, `dotnet run --project <sample>` — packages resolve from `nuget.org` directly, no private repo access needed.
- Update the "Release posture" section: Antiphon `v1.0.0` **is now published to nuget.org** under evaluation mode (no license key required, full function, notice-only — do not describe this as the full commercial/paid tier). The full commercial EULA still awaits a lawyer review pass before any paid license is sold; standards-completion work (Peppol 3.0.21, Factur-X/France) is still in progress for the full baseline — keep these caveats, just correct the "not yet published" framing since that part is no longer true. Do not claim more than is actually true (e.g. don't imply the paid tier is available, don't imply full standards conformance is complete).
- Remove the "you can still read every sample's source... but you can't currently build or run it" caveat — that limitation no longer applies once nuget.org restore works.

### 4. Update PACKAGES.md

Remove or rewrite the "Not on nuget.org yet" section to reflect that the packages are, in fact, on nuget.org now, at version `1.0.0`.

---

## Files Allowed to Change

- `NuGet.config` — remove the local feed source
- `01-generate-xrechnung/01-generate-xrechnung.csproj` — version bump
- `02-validate-peppol/02-validate-peppol.csproj` — version bump
- `03-facturx-pdf/03-facturx-pdf.csproj` — version bump
- `README.md` — remove local-feed instructions, update release posture
- `PACKAGES.md` — remove "not on nuget.org yet" section

This is the permitted change set. If you discover a necessary change outside this list, stop and report it rather than expanding scope silently.

---

## Files and Folders Out of Scope

- Do not modify any sample's `.cs` source files.
- Do not modify `docs/Results/` (any file).
- Do not modify `LICENSE`.
- Do not modify the `public` remote, `public-export` branch, or any dual-remote sync tooling.
- Do not modify sibling repositories (including `../Antiphon`).
- Do not modify Wolfgang.
- Do not create migrations (not applicable to this repo, stated for consistency with the standard template).
- Do not commit or push.

---

## Verification Steps

Confirm no `local-feed/` directory remains, then build and run each sample fresh against nuget.org only:

```bash
rm -rf local-feed
"/mnt/c/Program Files/dotnet/dotnet.exe" restore 01-generate-xrechnung/01-generate-xrechnung.csproj
"/mnt/c/Program Files/dotnet/dotnet.exe" run --project 01-generate-xrechnung

"/mnt/c/Program Files/dotnet/dotnet.exe" restore 02-validate-peppol/02-validate-peppol.csproj
"/mnt/c/Program Files/dotnet/dotnet.exe" run --project 02-validate-peppol

"/mnt/c/Program Files/dotnet/dotnet.exe" restore 03-facturx-pdf/03-facturx-pdf.csproj
"/mnt/c/Program Files/dotnet/dotnet.exe" run --project 03-facturx-pdf
```

Confirm each sample produces its expected real output (an `invoice.xml`, a validation report, a `facturx-output.pdf`) exactly as `EX-1`'s own result file describes — the point of this phase is proving the swap actually works end-to-end, not just that the file edits look plausible. If `dotnet restore` fails to find any package at `1.0.0` on nuget.org, STOP and report the exact error — do not silently pin to a different version or re-add a local feed to work around it.

If running from WSL and `dotnet` resolves to the Windows-side `dotnet.exe`, be aware this repo's samples are simple console apps with no localhost/HTTP dependency, so the WSL↔`dotnet.exe` networking caveat from `methodology/LESSONS_LEARNED.md` does not apply here — plain `dotnet run` output should be directly visible.

If verification cannot be completed due to an environment issue, report the exact command and failure. Do not claim success without evidence.

---

## Post-Completion Review (bounded — one round)

After verification passes, run **one** Codex review of the full diff for this phase. Then apply exactly **one** fix pass under these rules, and stop:

1. **Fix only findings with a concrete failure scenario** — specific inputs or state leading to wrong output, a crash, data loss, or a security hole. Style, naming, "consider refactoring," and preference findings are recorded, never acted on in this pass.
2. **One fix pass, no re-review loop.** After fixing, re-run the Verification Steps above. Do not request a second review round.
3. **Fixes stay inside the "Files Allowed to Change" list** and must keep all three samples running. A fix that needs a file outside that list is a follow-up item, not a fix.
4. If Codex is unavailable, say so in the completion report and skip this section — do not substitute an unbounded self-review.

---

## Documentation Updates

- [ ] `README.md` and `PACKAGES.md` are the primary documentation targets of this phase itself (see Scope above) — confirm both are internally consistent after editing (no leftover reference to `local-feed` or "not yet published" anywhere else in either file).
- [ ] If no other documentation change was needed, state that explicitly in the completion report.

---

## Result File

Save a copy of your completion report to:

```
docs/Results/EX-2-Real-Nuget-Swap.md
```

---

## Completion Report

When finished, report:

1. Summary — what changed and why.
2. Safety checks or pre-flight inspection results (if applicable).
3. Files changed — added, updated, or deleted.
4. Files not changed — list any from the "Files Allowed to Change" set that turned out not to need changes, and why.
5. Documentation changes — what was reviewed and what was updated, or why no update was needed.
6. Behavior and contract impact — confirm whether runtime behavior or public API changed (expected: no).
7. Database impact — confirm none (expected: not applicable to this repo).
8. Verification results — exact commands and outcomes, including confirmation each sample's real output was produced.
9. Review outcome — findings fixed (with the failure scenario each addressed), findings recorded-but-not-fixed (with severity), or "review skipped: [reason]".
10. Out-of-scope items discovered — flag but do not implement.
11. Suggested follow-up tasks — name them; Wolfgang will track them.

---

## After This Prompt Executes

Bring the completion report back to Wolfgang. Wolfgang will update:

- `projects/antiphon.md` — project card
- `ecosystem/ACTIVE_PRIORITIES.md` — close row 3a
- `ecosystem/PROJECT_REGISTRY.md` if phase or status changed
- `methodology/LESSONS_LEARNED.md` if a reusable lesson was identified
- `briefings/LEONARD_BRIEFING.md` if strategy changed
